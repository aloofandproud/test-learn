spring:
  application:
    name: ms-regist
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8880/eureka/
server:
  port: 8880
